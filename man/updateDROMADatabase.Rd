% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DROMA_SQLManager.R
\name{updateDROMADatabase}
\alias{updateDROMADatabase}
\title{Update DROMA Database with New Object}
\usage{
updateDROMADatabase(obj, table_name, overwrite = FALSE, connection = NULL)
}
\arguments{
\item{obj}{The object to add to the database (matrix or data.frame)}

\item{table_name}{The name to use for the table in the database}

\item{overwrite}{Logical, whether to overwrite if table already exists (default: FALSE)}

\item{connection}{Optional database connection object. If NULL, uses global connection}
}
\value{
Invisibly returns TRUE if successful
}
\description{
Adds or updates a table in the DROMA database with a new object
}
\examples{
\dontrun{
# Connect to database
con <- connectDROMADatabase("path/to/droma.sqlite")

# Create a matrix of gene expression data
expr_data <- matrix(rnorm(100), nrow = 10, ncol = 10)
rownames(expr_data) <- paste0("gene", 1:10)
colnames(expr_data) <- paste0("sample", 1:10)

# Add to database as "myproject_mRNA"
updateDROMADatabase(expr_data, "myproject_mRNA", overwrite = TRUE)
}
}
