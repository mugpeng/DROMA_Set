% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DromaSet-database.R
\name{createDromaSetFromDatabase}
\alias{createDromaSetFromDatabase}
\title{Create a DromaSet from Database}
\usage{
createDromaSetFromDatabase(
  projects,
  db_path = file.path(path.expand("~"), "droma.sqlite"),
  db_group = NULL,
  load_metadata = TRUE,
  dataset_type = NULL,
  auto_load = FALSE,
  con = NULL
)
}
\arguments{
\item{projects}{The name of the project/dataset (e.g., "gCSI", "CCLE")}

\item{db_path}{Path to the SQLite database}

\item{db_group}{Optional group name in the database, if different from projects}

\item{load_metadata}{Logical, whether to load sample and treatment metadata (default: TRUE)}

\item{dataset_type}{Optional dataset type (e.g., "CellLine", "PDX", "PDO")}

\item{auto_load}{Logical, whether to automatically load treatment response and molecular profiles (default: FALSE)}

\item{con}{Optional database connection object. If provided, this connection will be used instead of creating a new one}
}
\value{
A DromaSet object linked to the database
}
\description{
Creates a DromaSet object linked to data in a SQLite database
}
\examples{
\dontrun{
# Create a DromaSet for gCSI data from database
gCSI <- createDromaSetFromDatabase("gCSI", "~/droma.sqlite")

# Create a DromaSet and automatically load drug response data
gCSI <- createDromaSetFromDatabase("gCSI", "~/droma.sqlite", auto_load = TRUE)
}
}
