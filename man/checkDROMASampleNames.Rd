% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DROMA_SQLManager.R
\name{checkDROMASampleNames}
\alias{checkDROMASampleNames}
\title{Check and Harmonize Sample Names Against DROMA Database}
\usage{
checkDROMASampleNames(
  sample_names,
  connection = NULL,
  max_distance = 0.2,
  min_name_length = 5
)
}
\arguments{
\item{sample_names}{Character vector of sample names to check and harmonize}

\item{connection}{Optional database connection object. If NULL, uses global connection}

\item{max_distance}{Numeric, maximum distance for fuzzy matching (default: 0.2)}

\item{min_name_length}{Integer, minimum name length for partial matching (default: 5)}
}
\value{
A data frame with columns: original_name, cleaned_name, harmonized_name, match_type, match_confidence, new_name
}
\description{
Checks sample names (column names) against the sample_anno table in the DROMA database
and provides harmonized mappings. Uses fuzzy matching and name cleaning similar to the approach
in 02-harmonized_name.Rmd.
}
\examples{
\dontrun{
# Connect to database
con <- connectDROMADatabase("path/to/droma.sqlite")

# Check sample names from a data matrix
sample_names <- colnames(my_data_matrix)
name_mapping <- checkDROMASampleNames(sample_names)

# View the mapping results
print(name_mapping)
}
}
